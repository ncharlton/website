{% extends 'admin/admin_base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit">Attach</button>
    {{ form_end(form) }}
    <hr>
    <a href="{{ path('admin_video_playlist_remove', {'slug' : video.slug}) }}">Remove video from playlist</a>
{% endblock %}