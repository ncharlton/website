{% extends 'admin/admin_base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    Do you really want to unblock {{ user.username }}?
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Delete</button>
    {{ form_end(form) }}
{% endblock %}