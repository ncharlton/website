{% extends 'admin/admin_base.html.twig' %}

{% block title %}Unblock {{ user.username }}{% endblock %}

{% block pageheader %}Unblock user{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <div class="block">
                <div class="title"><strong class="d-block">Unblock {{ user.username }}</strong></div>
                <div class="block-body">
                    <p>Do you really want to unblock {{ user.username }}?</p>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}

                    <button class="btn btn-primary" type="submit">Unblock</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}