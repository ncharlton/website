{% extends 'admin/admin_base.html.twig' %}

{% block title %}Delete - {{ map.title }}{% endblock %}

{% block pageheader %}Delete Map{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <div class="block">
                <div class="title"><strong class="d-block">Delete Map</strong></div>
                <div class="block-body">
                    <p>Are you sure you want to delete {{ map.title }}?</p>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}

                    <button class="btn btn-primary" type="submit">Delete</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}